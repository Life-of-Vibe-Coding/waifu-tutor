# Frontend env (copy to .env if needed)
# VITE_API_BASE_URL=http://localhost:8000
# Only set if not using Vite proxy (e.g. production build)
# VITE_DEV_PROXY_TARGET=http://localhost:8000
# Optional for `npm run dev`: override Vite proxy target when backend uses a non-default port
