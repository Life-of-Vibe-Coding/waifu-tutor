# Backend env (copy to .env and fill in)
# cp .env.example .env

# Database (relative to backend/ when running uvicorn)
# DATABASE_URL=sqlite:///../db/data/waifu_tutor.db
# UPLOAD_DIR=../db/data/uploads
# MAX_UPLOAD_BYTES=10485760

# Volcengine ARK (chat)
# VOLCENGINE_API_KEY=
# VOLCENGINE_CHAT_BASE=https://ark.cn-beijing.volces.com/api/v3
# CHAT_MODEL=doubao-seed-2-0-mini-260215

# OpenViking (context DB; config defaults to project .openviking/ov.conf, data to db/data/openviking)
# OPENVIKING_CONFIG_FILE=  (path to ov.conf; default: <project>/.openviking/ov.conf)
# OPENVIKING_DATA_DIR=     (path to context DB; default: <project>/db/data/openviking)

# Gmail (optional)
# GMAIL_CLIENT_ID=
# GMAIL_CLIENT_SECRET=
# GMAIL_REDIRECT_URI=http://localhost:5173 (or your frontend URL)
# GMAIL_SUCCESS_URL=/
