# Next.js app (single server)
# DATABASE_URL=file:./data/waifu_tutor.db
# UPLOAD_DIR=./data/uploads
# MAX_UPLOAD_BYTES=10485760
# EMBEDDING_DIM=1536

# Gemini (optional; fallbacks work without key)
# GEMINI_API_KEY=
# GEMINI_MODEL=gemini-2.0-flash
# GEMINI_EMBED_MODEL=text-embedding-004

# Qwen/DashScope: chat, summarize, flashcards, organize, subject classification, reranker (priority over Gemini when set).
# DASHSCOPE_API_KEY=
# DASHSCOPE_BASE=https://dashscope.aliyuncs.com
# QWEN_MODEL=qwen-flash
# RAG reranker (after fast retrieval): qwen3-rerank or gte-rerank-v2
# RERANK_MODEL=qwen3-rerank

# Playwright remote (login-required sites e.g. ntulearn): launch Chrome with remote debugging.
# Use your existing Chrome profile (e.g. maythegod1772@gmail.com) so you're already logged in:
#   macOS:   "/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" --remote-debugging-port=9222 --user-data-dir="$HOME/Library/Application Support/Google/Chrome" --profile-directory="Default"
#   (If you have multiple profiles, get the folder name from chrome://version → "Profile Path"; use that after --profile-directory=)
# Or use a clean profile: --user-data-dir="$HOME/.waifu-chrome-debug"
# CHROME_CDP_URL=http://127.0.0.1:9222
# CHROME_USER_DATA_DIR=   # override Chrome user data dir (default: platform default)
# CHROME_PROFILE_DIRECTORY=Profile 1   # profile folder name, e.g. Default or "Profile 1" (in shell: export CHROME_PROFILE_DIRECTORY="Profile 1")
# CHROME_RECORD_PROFILE_DIR=   # when main Chrome is in use, Record uses this persistent profile dir (default: ./data/chrome-record-profile)
# RECORDINGS_DIR=./data/recordings

# Gmail (read inbox, send mail) - Web app integration
# Register: https://console.cloud.google.com/ → Gmail API → OAuth client ID (Web)
# GMAIL_CLIENT_ID=
# GMAIL_CLIENT_SECRET=
# GMAIL_REDIRECT_URI=http://localhost:3000/api/gmail/callback
# GMAIL_SUCCESS_URL=/
